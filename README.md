
## Student Details
1.Name : NanthaKumara A/L Ganesan
2.Student id : 106214014
3.Title : Deployment task 1 (SWE40006)

## Deployment Steps
1. Clone or download the repository.
2. Open `MathLibrary.sln` in Visual Studio.
3. Build the solution (`Ctrl + Shift + B`) to restore and generate `bin`/`obj` build outputs.
4. Compile `Product.wxs` via the WiX extension to produce `WixInstaller.msi`.
